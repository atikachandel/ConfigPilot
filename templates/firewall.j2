! Generated by ConfigPilot for {{ device_name }} ({{ environment | default('unknown') }})
hostname {{ device_name }}
!
{% for zone in zones %}
zone {{ zone.name }}
 interfaces {{ zone.interfaces | join(', ') }}
!
{% endfor %}
{% for rule in rules %}
rule {{ rule.action }} {{ rule.source }} {{ rule.destination }} {{ rule.service | default('any') }}
{% endfor %}
rule deny any any
!
snmp-server community {{ snmp_community }} RO
logging host {{ syslog_server }}
